% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{pg_offset_grid}
\alias{pg_offset_grid}
\title{Shift a PRIO-GRID configuration by a distance and bearing}
\usage{
pg_offset_grid(
  config = pg_current_config(),
  distance_km = 5,
  bearing_deg = 90,
  reference_lat = NULL
)
}
\arguments{
\item{config}{A \code{pg_config} object. Defaults to \code{\link{pg_current_config}()}.}

\item{distance_km}{Numeric. Distance to shift, in kilometres. Default 5.}

\item{bearing_deg}{Numeric. Compass bearing in degrees: 0 = north,
90 = east (default), 180 = south, 270 = west.}

\item{reference_lat}{Numeric. Latitude in degrees used to convert the
east-west component from kilometres to degrees of longitude. Only used
for a geographic CRS. Defaults to the vertical midpoint of
\code{config$extent}.}
}
\value{
A new \code{pg_config} object with \code{extent} shifted.
}
\description{
Returns a copy of \code{config} with its \code{extent} translated by
\code{distance_km} kilometres along compass bearing \code{bearing_deg}.
\code{nrow}, \code{ncol} and \code{crs} are left unchanged, so the
resulting grid is congruent to the original (same cell size and count),
just moved. Pass the result to \code{\link{prio_blank_grid}()}, or as
\code{config} to \code{\link{calc_pg}()} / \code{\link{build_pg_dataset}()},
to build the same PRIO-GRID variables on the shifted grid.
}
\details{
This is meant for testing the sensitivity of PRIO-GRID-based analysis to
the modifiable areal unit problem (MAUP): the underlying source data is
identical, but shifting the grid the data is aggregated onto changes which
raw pixels fall into which cell, and therefore the aggregated values.

For a geographic CRS (\code{config$crs} of \code{"epsg:4326"}), the shift
is converted from kilometres to degrees using a spherical-earth
approximation (111.32 km per degree of latitude, scaled by
\code{cos(reference_lat)} for longitude). Because a degree of longitude
covers less ground away from the equator, the same kilometre offset yields
a different degree offset depending on latitude; \code{reference_lat}
picks the latitude the offset should be exact at (default: the vertical
midpoint of \code{config$extent}). For any other CRS, \code{distance_km}
is applied directly assuming metre-based map units (true of most
projected CRSs).

Shifting a grid whose extent already spans the full globe
(\code{xmin = -180, xmax = 180}) leaves a sliver of the globe uncovered at
one edge and unreachable at the other, since \code{xmin}/\code{xmax} no
longer sit exactly on the antimeridian. This does not matter for a config
scoped to a country or region, which is the typical MAUP use case.
}
\examples{
cfg <- pg_config(extent = c(xmin = 0, xmax = 10, ymin = 45, ymax = 55))
offset_cfg <- pg_offset_grid(cfg, distance_km = 5, bearing_deg = 90)
offset_cfg$extent - cfg$extent
}
